# ==============================================================================
# 1. SISTEMA OPERACIONAL (macOS e Windows)
# ==============================================================================
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
._*
.Trash-*
.VolumeIcon.icns
# (Para evitar sujeira caso um pendrive ou pasta compartilhada do Windows seja lida)
Thumbs.db
ehthumbs.db
Desktop.ini

# ==============================================================================
# 2. EDITORES E IDEs (VS Code e Neovim)
# ==============================================================================
# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace
.history/

# Neovim / Vim
*.swp
*.swo
*~
Session.vim
.netrwhist

# ==============================================================================
# 3. SEGURANÇA E AMBIENTE (Crucial)
# ==============================================================================
# Garante que arquivos com chaves e senhas NUNCA subam,
# a menos que o projeto permita explicitamente.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
